Install guides for my computers
================================

* Update and upgrade everything.
* Create an ssh key (ssh-keygen -t rsa) and add to GitHub.
* Install Dropbox and setup firefox bookmarks.
* Install git, add 'github/dotfiles'.
* Install zsh and zpretzo.
* Install neovim
* Copy relevant dotfiles
* Add .condarc and conda install environment called 'default':

```bash
~/miniconda3/bin/conda create -n default python=3.*
```

* Install Atom and add keymap
* Setup and Dropbox.
* Install NVIM, add init.vim and install Vundle
* Install software, such as
	- Terminator
	- PyCharm/Webstorm/Clion (add settings from dropbox), 
	- Skype, Telegram, Signal
	- Firefox, Thunderbird,
	- Cuda/Nvidia driver

* Add bookmarks to Firefox.
* Clone my repositories using SSH

